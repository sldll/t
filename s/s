SINK=$(pactl info | awk -F': ' '/Default Sink/ {print $2}')

case "$SINK" in
    *pci-0000_00_1f*) SHORT="spkr" ;;
    *pci-0000_00_1b*) SHORT="spkr" ;;
    *Headset*) SHORT="usbc" ;;
    *hdmi*) SHORT="hdmi" ;;
    *bluez_output.*)    SHORT="bluez" ;;
esac

echo $SHORT
